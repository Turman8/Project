# Core numerical and scientific stack
numpy==1.23.5
scipy==1.10.1
pandas==1.5.3
matplotlib==3.7.2

# Signal processing utilities
PyWavelets==1.4.1
wfdb==4.1.2

# Machine learning
scikit-learn==1.3.2
tensorflow==2.10.1

# Optional components (install on demand)
# tflite-runtime==2.14.0      # 在 Pynq-Z2 或其他 ARM 设备上执行量化模型时需要
# tensorflow-gpu==2.10.1      # 若使用 pip/Conda 独立 GPU 版本，可替代上面的 tensorflow

